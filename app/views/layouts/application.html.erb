<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Charlotte City Connect" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag :application, media: 'all', "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="<%= controller.controller_name %>-<%= controller.action_name %>">

    <!-- Navigation bar -->
    <header style="
      width: 100%;
      background-color: #004aad;
      padding: 0.75rem 1.5rem;
      display:flex;
      align-items:center;
      justify-content:space-between;
      position: relative;
      top: 0;
      left: 0;
      z-index: 1000;
    ">

      <!-- Left side -->
      <div style="display:flex; align-items:center; gap:2rem;">
        <div style="color:white; font-weight:700; font-size:1.3rem;">
          Charlotte City Connect
        </div>

        <nav style="display:flex; align-items:center; gap:1rem;">
          <%= link_to 'Home', root_path, style: "color:white; text-decoration:none; font-weight:500;" %>
          <%= link_to 'Events', events_path, style: "color:white; text-decoration:none; font-weight:500;" %>
          <%= link_to 'My Calendar', calendar_path,
      style: "color:white; text-decoration:none; font-weight:500;" %>
          <%= link_to 'Create Event', new_event_path, style: "color:white; text-decoration:none; font-weight:500;" %>
<%= link_to "My Created Events",
            my_created_events_path,
            style: "color:white; text-decoration:none; font-weight:500;" %>


        </nav>
      </div>

      <!-- Right side -->
      <div style="display:flex; align-items:center; gap:1rem;">
        <% if signed_in? %>
          <span style="color:white; font-weight:600;">Hello, <%= current_user&.name %></span>

          <%= button_to 'Log Out', logout_path,
                method: :delete,
                data: { confirm: 'Log out?' },
                class: "text-white font-bold px-2 py-1 bg-transparent border-0" %>

        <% else %>
          <%= link_to 'Log In', login_path,
              style: "color:white; text-decoration:none; font-weight:bold;" %>

          <%= link_to 'Sign Up', signup_path,
              style: "color:white; text-decoration:none; font-weight:bold;" %>
        <% end %>
      </div>

    </header>

    <!-- ⭐ FLASH ALERT BOX ⭐ -->
    <% if flash[:alert] %>
      <div style="
        background-color: #ffdddd;
        color: #a10000;
        padding: 12px 16px;
        margin: 10px auto;
        max-width: 1100px;
        border: 1px solid #cc0000;
        border-radius: 6px;
        font-weight: 600;
      ">
        <%= flash[:alert] %>
      </div>
    <% end %>

    <!-- ⭐ CENTERED PAGE CONTENT ⭐ -->
    <main style="max-width: 1100px; margin: 0 auto; padding: 2rem;">
      <%= yield %>
    </main>

  </body>
</html>
